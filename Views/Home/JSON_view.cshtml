@using Web_CSV_Json_XML_reader.Models
@using Web_CSV_Json_XML_reader.Controllers
@model JSONViewModel;
@{
    int i = 0;
    int j = 0;
    int j_max = 0;
}
@{/*
<!DOCTYPE html>
<html>
<head>
    <title>JSON Reader</title>
    <meta charset="utf-8" />
</head>
<body>
    
    @if (Model != null)
    {
        <form method="post" action="/Home/JSONSave">

            <input type="submit" name="Save" value="Send">
            <table border="1" cellpadding="1" cellspacing="1" align="center">
            @foreach (var property in Model.Data.Properties())
            {
                //exit += property.Name + ": " + property.Value + '\n';
                <tr>
                    <td><input name="cell[@i,@j]" value="@property.Name"></td>
                    
                    @if (property.Value.Count() > 1)
                    {
                        j++;
                        @foreach (var cell in property.Value)
                        {
                            <td style="display: none;"><input name="cell[@i,@j]_type" value="@property.Value.Type.ToString()"></td>
                            <td><input name="cell[@i,@j]" value="@cell.ToString()"></td>
                            j++;
                        }
                    }
                    else
                    {
                        j++;
                        <td style="display: none;"><input name="cell[@i,@j]_type" value="@property.Value.Type.ToString()"></td>
                        <td><input name="cell[@i,@j]" value="@property.Value"></td>
                        j++;
                    }
                    
                </tr>
                    @if (j_max < j) j_max = j;
                    i++;
                    j = 0;



            }
                <input type="number" value="@j_max" name="j_lenght">
                <input type="number" value="@Model.Data.Count" name="i_lenght">
            </table>
        </form>
    }*/
    
<!--</body>
</html>-->
    }


